@{
    ViewData["Title"] = "Проверка кода";
}
<div class="auth-page">
    <div class="auth-card">
        <h2>Проверка кода</h2>
        <p style="font-size:0.85rem; color:#666; text-align:center; margin-bottom:20px;">Введите 6-значный код, высланный для сброса пароля.</p>

        @if (TempData["Error"] != null)
        {
            <div style="color:red; font-size:0.85rem; margin-bottom:15px; text-align:center;">@TempData["Error"]</div>
        }

        <form asp-action="VerifyResetCode" method="post">
            @Html.AntiForgeryToken()
            <div class="auth-form-group">
                <label>Код восстановления</label>
                <input name="code" type="text" class="auth-input" placeholder="000000" maxlength="6" required style="text-align:center; letter-spacing: 5px; font-size: 1.2rem;" />
            </div>
            <button type="submit" class="btn-auth">Проверить код</button>
        </form>
    </div>
</div>